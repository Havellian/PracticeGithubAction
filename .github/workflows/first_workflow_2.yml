name: first_workflow
on: push
jobs:
  lint_test_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: All steps together
        run: |
              npm ci
              npm run lint
              npm test
              npm run build